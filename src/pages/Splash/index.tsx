import React from 'react';
import {View, Text} from 'react-native';
import Styles from '../../styles';

const Splash = () => (
  <View style={Styles.container}>
    <Text style={Styles.textTitle}>Splash Screen</Text>
  </View>
);

export default Splash;
